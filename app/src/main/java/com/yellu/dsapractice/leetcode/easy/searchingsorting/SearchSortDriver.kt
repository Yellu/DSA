package com.yellu.dsapractice.leetcode.easy.searchingsorting

fun main() {
//    val obj = MergeSortedArray()
//    obj.merge(intArrayOf(1, 2, 3), 3, intArrayOf(2, 5, 6), 3)

    val firstBadVersion = FirstBadVersion()
    val version = firstBadVersion.badVersion(10)
    println(version)
}